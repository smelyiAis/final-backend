<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Signup - Formula House</title>
        <%- include('components/head.ejs') %>
        <style>
            body {
                background: url("/images/login_bg.jpg") no-repeat center center fixed;
                background-size: cover;
            }
        </style>
    </head>
    <body>
        <%- include('components/navbar.ejs') %>

        <div class="d-flex flex-column justify-content-between vh-100">
            <div class="login-container">
                <form class="login-form rounded" method="POST" action="/auth/signup">
                    <h2 class="text-center">Sign up</h2>
                    <% if (error) { %>
                    <div class="alert alert-danger" role="alert"><%= error %></div>
                    <% } %>
                    <div class="mb-3">
                        <label for="username" class="form-label">Username</label>
                        <input type="text" class="form-control" id="username" name="username" placeholder="Fernando Alonso" required />
                    </div>
                    <div class="mb-3">
                        <label for="email" class="form-label">Email</label>
                        <input type="text" class="form-control" id="email" name="email" placeholder="fernadoalonso@formulaone.com" required />
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" class="form-control" id="password" name="password" placeholder="thebigboss228" required />
                    </div>
                    <div class="mb-3">
                        <label for="repeat_password" class="form-label">Repeat password</label>
                        <input type="password" class="form-control" id="repeat_password" name="repeat_password" placeholder="thebigboss228" required />
                    </div>
                    <button type="submit" class="btn btn-danger w-100">Signup</button>
                    <div class="mt-2">Already registered? <a href="/signup">Log in now!</a></div>
                    <div class="mt-2">By accessing and using our website,<br />you agree to our Terms of Use.</div>
                </form>
            </div>

            <%- include('components/footer.ejs') %>
        </div>
    </body>
</html>
